{
  "name": "user_service",
  "version": "1.0.0",
  "description": "user service for peerprep",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "dotenv -e .env -- ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "format": "prettier --write \"src/*.ts\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "dotenv-cli": "^10.0.0",
    "ts-node-dev": "^2.0.0"
  }
}
