name: Root CI Pipeline
run-name: ${{ github.actor }} is learning GitHub Actions
on:
  pull_request:
    branches:
      - '**'
jobs:
  run-backend:
    uses: ./backend/.github/workflows/.backend_ci.yml@main
