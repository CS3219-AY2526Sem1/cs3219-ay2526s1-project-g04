apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: collab-service
  namespace: default
spec:
  parentRefs:
    - name: gateway
      sectionName: http
  # hostnames:
  # - "cafe.example.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/collab-service
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: collab-service
          port: 3009
