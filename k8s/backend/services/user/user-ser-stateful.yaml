apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-us
spec:
  serviceName: "postgres-us"
  replicas: 1
  selector:
    matchLabels:
      app: postgres-us
  template:
    metadata:
      labels:
        app: postgres-us
    spec:
      containers:
        - name: postgres-us
          image: postgres
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-us-secret
                  key: POSTGRES_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: postgres-us-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: postgres-us-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi
        storageClassName: gp2
